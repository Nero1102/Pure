# Project pitch

This folder is a lightweight review pack for the `pure` runtime.

## Architecture map

See `docs/architecture/agent-harness-v1-overview.md`.

## Benchmark evidence

Benchmark artifacts are produced under `artifacts/` and `.pure/runs/`.

## Sample run artifact list

- `.pure/runs/<run_id>/task_state.json`
- `.pure/runs/<run_id>/trace.jsonl`
- `.pure/runs/<run_id>/report.json`
